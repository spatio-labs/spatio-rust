/*
 * SpatioAPI
 *
 * The REST API that owns every resource in your Spatio workspace: notes, sheets, slides, tasks, calendar events, mail, chat, files, and contacts. SpatioMCP wraps this API; Spatio Desktop reads from it. You can call it directly from your own code.  All requests must be authenticated with a Personal Access Token (`Authorization: Bearer pat_...`) or an OAuth 2.1 access token, and use HTTPS.  Official SDKs (MIT, generated from this spec on every release):  - TypeScript: https://github.com/spatio-labs/spatio-ts (`npm install @spatio-labs/spatio-ts`) - Python: https://github.com/spatio-labs/spatio-py (`pip install spatio-sdk`) - Go: https://github.com/spatio-labs/spatio-go (`go get github.com/spatio-labs/spatio-go`)  This specification is generated from the platform-service Go source on every push to `main`. The spec, not hand-written documentation, is the source of truth: server stubs and SDKs are generated from it, and any drift between the spec and the running service fails CI. 
 *
 * The version of the OpenAPI document: v1
 * Contact: hello@spatio.app
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// ChunkedFileManifest : Block-level manifest for a chunked-uploaded file. Returned by `GET /v1/files/{id}/manifest`. Only meaningful for files uploaded via the chunked path; legacy uploads return `404`. 
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct ChunkedFileManifest {
    #[serde(rename = "manifestId")]
    pub manifest_id: String,
    #[serde(rename = "fileId")]
    pub file_id: String,
    #[serde(rename = "fileName")]
    pub file_name: String,
    #[serde(rename = "version")]
    pub version: i32,
    #[serde(rename = "totalSize")]
    pub total_size: i64,
    #[serde(rename = "blockCount")]
    pub block_count: i32,
    #[serde(rename = "chunkingAlgorithm", skip_serializing_if = "Option::is_none")]
    pub chunking_algorithm: Option<String>,
    #[serde(rename = "fileChecksum", skip_serializing_if = "Option::is_none")]
    pub file_checksum: Option<String>,
    #[serde(rename = "blocks", skip_serializing_if = "Option::is_none")]
    pub blocks: Option<Vec<std::collections::HashMap<String, serde_json::Value>>>,
}

impl ChunkedFileManifest {
    /// Block-level manifest for a chunked-uploaded file. Returned by `GET /v1/files/{id}/manifest`. Only meaningful for files uploaded via the chunked path; legacy uploads return `404`. 
    pub fn new(manifest_id: String, file_id: String, file_name: String, version: i32, total_size: i64, block_count: i32) -> ChunkedFileManifest {
        ChunkedFileManifest {
            manifest_id,
            file_id,
            file_name,
            version,
            total_size,
            block_count,
            chunking_algorithm: None,
            file_checksum: None,
            blocks: None,
        }
    }
}

